pluginManagement {
    repositories {
        maven { url "http://${nexusHost}:${nexusPort}/repository/maven-public" }
    }
}

rootProject.name = "thule-cloud"

include "thule-cloud-config"
include "thule-cloud-service"
