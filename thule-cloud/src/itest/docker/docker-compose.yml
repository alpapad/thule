version: "3.5"
services:
  ################################  thule-admin-server  ################################
  thule-cloud-admin-server:
    image: pooh:8082/thule-admin-server
    environment:
      THULE_CONFIG_SERVICE_HOST: thule-cloud-config-service
      THULE_DISCOVERY_SERVICE_HOST: thule-cloud-discovery-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: admin-server-1
    ports:
      - 9071:8071
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro

  ################################  thule-config-service  ################################
  thule-cloud-config-service:
    image: pooh:8082/thule-config-service
    environment:
      THULE_DISCOVERY_SERVICE_HOST: thule-cloud-discovery-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: config-service-1
    ports:
      - 9888:8888
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro

  ################################  thule-discovery-service  ################################
  thule-cloud-discovery-service:
    image: pooh:8082/thule-discovery-service
    environment:
      THULE_CONFIG_SERVICE_HOST: thule-cloud-config-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: discovery-service-1
    ports:
      - 9761:8761
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro

  ################################  thule-edge-server  ################################
  thule-cloud-edge-server:
    image: pooh:8082/thule-edge-server
    environment:
      THULE_CONFIG_SERVICE_HOST: thule-cloud-config-service
      THULE_DISCOVERY_SERVICE_HOST: thule-cloud-discovery-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: edge-server-1
    ports:
      - 9080:8080
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro

  ################################  thule-email-server  ################################
  thule-cloud-email-service:
    image: pooh:8082/thule-email-service
    environment:
      THULE_CONFIG_SERVICE_HOST: thule-cloud-config-service
      THULE_DISCOVERY_SERVICE_HOST: thule-cloud-discovery-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: email-server-1
    ports:
      - 9091:8091
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro

  ################################  thule-people-service  ################################
  thule-cloud-people-service:
    image: pooh:8082/thule-people-service
    environment:
      THULE_CONFIG_SERVICE_HOST: thule-cloud-config-service
      THULE_DISCOVERY_SERVICE_HOST: thule-cloud-discovery-service
      TZ: Europe/London
    extra_hosts:
      - logstash-host:172.17.0.1
      - zipkin-host:172.17.0.1
    hostname: people-service-1
    ports:
      - 9090:8090
    volumes:
      # Set localtime on container to be the same as the host
      - /etc/localtime:/etc/localtime:ro