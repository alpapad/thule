pluginManagement {
    repositories {
        maven { url "http://${nexusHost}:${nexusPort}/repository/maven-public" }
    }
}

rootProject.name = "thule-configuration"

include "thule-configuration-config"
include "thule-configuration-service"
