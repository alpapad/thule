# Spring boot configuration
spring:
  datasource:
    password: thule
    username: thule
  flyway:
    password: root
    user: root

# Thule configuration
thule:
  peopleservice:
    mysql:
      schema: thule
