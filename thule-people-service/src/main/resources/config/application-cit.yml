# Spring boot configuration
spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            token-uri: https://piglet/thule-authentication-service/auth/realms/thule/protocol/openid-connect/token
      resourceserver:
        jwt:
          issuer-uri: https://piglet/thule-authentication-service/auth/realms/thule
