version: "3.7"
services:
  ################################  Servers and Databases Required By Integration Tests  ################################
  mysql:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      TZ: Europe/London
    ports:
      - 3307:3306 # Use 3306 for dev, 3307 for itest and 3308 for ctest so they can all execute simultaneously if required

networks:
  - mysql
