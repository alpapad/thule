# Spring boot configuration
spring:
  cloud:
    kubernetes:
      discovery:
        enabled: false
  mail:
    host: localhost
    port: 1025
    username: test.user
    password: test.user
  zipkin:
    enabled: false

# Thule configuration
thule:
  shared:
    logging:
      logstash:
        enabled: false
    oauth2:
      resourceserver:
        jws:
          enabled: false
