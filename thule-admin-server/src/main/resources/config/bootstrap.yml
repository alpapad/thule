# Bootstrap
spring:
  application:
    name: thule-admin-server
  profiles:
    include: native

eureka:
  instance:
    nonSecurePort: ${server.port:8888}
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/